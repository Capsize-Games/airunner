# server/src

Current home of the `airunner_services` package.

This directory is now the real home of the `airunner_services` package.
Checkout workflows now use `server/src/airunner_services/` directly.
It also owns the service CLI modules and Alembic migration assets.