name: Docker Release

on:
  push:
    branches:
      - master
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        include:
          - name: headless
            profiles: core,llm-native,stt-native,art-python,tts-python
            tags: |
              ghcr.io/capsize-games/airunner/airunner:linux-headless
          # `latest` is included below deliberately: `docker pull
          # ghcr.io/capsize-games/airunner/airunner` with no tag resolves to
          # :latest, which did not exist -- so the obvious command every
          # reader tries returned "manifest unknown". The desktop build is
          # the right default for that. (Note: inside a `|` block scalar, a
          # line starting with `#` is literal string content, not a YAML
          # comment -- keep any explanatory comments outside the block, or
          # they become part of the tags list and break every push here.)
          - name: desktop
            profiles: core,llm-native,stt-native,art-python,tts-python,gui,computer-use
            tags: |
              ghcr.io/capsize-games/airunner/airunner:linux-desktop
              ghcr.io/capsize-games/airunner/airunner:linux
              ghcr.io/capsize-games/airunner/airunner:latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Build and push ${{ matrix.name }} image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          platforms: linux/amd64
          tags: ${{ matrix.tags }}
          build-args: |
            AIRUNNER_ENABLE_CUDA=0
            AIRUNNER_INSTALL_PROFILES=${{ matrix.profiles }}
