import Card from "react-bootstrap/Card";

export default function SettingsView() {
  return (
    <Card body>
      <h2>Settings</h2>
      <p className="text-muted">
        Model configuration, TTS, STT, and path settings will be built in
        Phase 4.
      </p>
    </Card>
  );
}
