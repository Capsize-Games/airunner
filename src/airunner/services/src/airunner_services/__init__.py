"""Service-side AIRunner package.

This package owns the Python daemon, lifecycle, and service-management
implementation. The legacy ``airunner.services`` package remains as a
compatibility namespace while callers are migrated.
"""
