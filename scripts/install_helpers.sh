#!/usr/bin/env bash
# DEPRECATED: All functionality has been merged into scripts/install.sh.
# This file exists only for backward compatibility.
# shellcheck source=/dev/null
source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/install.sh"
